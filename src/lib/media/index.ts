import { MediaImage } from "./image";
import { MediaRoot } from "./root";
import { MediaVideo } from "./video";

export const Media = {
  Image: MediaImage,
  Root: MediaRoot,
  Video: MediaVideo,
};
